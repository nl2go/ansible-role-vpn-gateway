# This file holds shared secrets or RSA private keys for authentication.

{{ ansible_managed|comment }}

{% for config in vpn_gateway_configs %}
{{ config.remote.public | ipaddr }} : PSK {{ config.psk }}
{% endfor %}
